[pytest]
testpaths = tests
# tests/ sits on the path so conftest helpers import by name, and the repo root sits on
# it so `server` and `rover` import the way they do in production.
pythonpath = . tests
addopts = -q --strict-markers
